import React from 'react'
import { Button } from '../../components/ui/button'
import { Metadata } from 'next'

export const metadata:Metadata={
  title :'Dashboard | Rafet',
  description:'Dashboard page',
  keywords:'dashboard, page'

}

const page = () => {
  return (
<Button></Button>
  )
}

export default page